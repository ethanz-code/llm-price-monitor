# llm-price-monitor 环境变量模板
# 复制为 .env 即可（compose 会把 .env 挂进 api 容器），无需在此填写任何密钥：
# AI Key、WxPusher Token 都在管理面板（Setup 向导 / 系统设置）里配置，保存在 var/monitor.db。
# .env 不进仓库（.gitignore 已排除）。

# 可选：首次启动且库中还没有管理员时，用下面的凭据自动建号（不设置则走 /setup 页面创建）。
# PRICE_WEB_USERNAME=admin
# PRICE_WEB_PASSWORD=change-me
